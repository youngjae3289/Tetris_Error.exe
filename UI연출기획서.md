
# 🖥️ UI 연출 기획서: Start UI (v1.2.8)
**Tetris_Error.exe**의 시작 화면 연출 규칙을 정의한다. 본 문서는 테마 기획서와 `main.html`의 구조/톤을 기반으로 하며, 값은 프리뷰 단계에서 조정 가능하도록 범위와 기본값을 함께 제시한다.

---

## 1. 목적 (Goal)
- 게임 시작 시 즉각적인 테마 몰입(사이버 오류/CRT/글리치 + **도트/픽셀 레트로**).
- 시작 버튼 클릭까지 시선을 고정할 수 있는 시각적 리듬 제공.
- 게임 시작 트랜지션은 **글리치 파열 + 흰색 플래시**로 명확하게 전환.

---

## 2. 시퀀스 타임라인 (Timeline, 기본값)
> **프리뷰에서 조정 가능한 기본값**으로 설정한다.

1) **Tetris 타이틀 등장**
	- 지속: **0.8s**
	- 효과: 페이드 인 + 약한 스케일(1.05 → 1.0) + 미세 CRT 노이즈

2) **글리치 전환 구간**
	- 지속: **0.6s**
	- 효과: 텍스트 수평 분리(채널 분리), 스캔라인 깨짐, 짧은 지터

3) **_Error.exe 추가 + 색상 전환**
	- 지속: **0.4s**
	- 효과: 뒤에 `_Error.exe`가 붙으며 전체 색상이 **붉은 계열**로 이동

4) **Idle 루프**
	- 지속: 무한
	- 효과: 약한 글리치(2~4초 간격 랜덤), 배경 에러 블록 비 내림 유지

---

## 3. 타이틀 타이포 & 스타일

### 3.1 폰트 구성
- **타이틀 전용 폰트**: **도트/픽셀 계열 폰트** (Press Start 2P 확대 사용 또는 동일 계열)
- **UI 라벨**: 기존 `Press Start 2P` 유지

### 3.2 타이틀 스타일 ("Tetris_Error.exe")
- **사이즈**: 화면 폭의 70~80% 범위 내 최대 크기
- **두께**: Bold/Black 수준
- **색상 변화**
  - 초기: Accent 계열 (분홍/흰색 혼합 톤)
  - 전환 후: **붉은 경고 톤**으로 이동 (#ff0000 ~ #8B0000 계열)

### 3.3 내부 글리치 텍스처
- **텍스처 유형**: **도트 매트릭스 + CRT 노이즈** 혼합 패턴
- **적용 방식**
  - 글자 내부에만 노이즈가 스며드는 느낌
  - 주기적으로 작은 라인 깨짐(1~2px) 발생
- **강도**: 가독성 유지 범위에서 **10~25% 투명도**

### 3.4 픽셀 룰
- 모든 타이틀/이펙트는 **픽셀 스냅(2~4px)** 을 적용해 부드러운 블러를 제거
- 타이틀 외곽에 **블록형 그림자**(1~3px) 적용해 도트 감각 강화

---

## 4. 글리치 효과 규칙 (Title Glitch)

- **채널 분리**: R/G/B 채널을 1~3px씩 좌우로 흔들어 분리
- **수평 지터**: 프레임당 0~2px 랜덤 이동
- **스캔라인 붕괴**: 글자 내부 가로 라인이 1~2줄씩 끊기는 느낌
- **Glitch Burst 타이밍**
  - 전환 구간(0.6s)에서 집중 발생
  - Idle 상태에서는 2~4초 간격 랜덤

---

## 5. 배경 연출: 에러 블록 레인

### 5.1 동작 개념
- **붉은 에러 블록**들이 **천천히 떨어지는** 배경 레이어
- 실제 게임 보드와 별개로, **패턴이 랜덤**하게 생성됨

### 5.2 패턴 규칙
- 패턴은 4x10 느낌을 유지하되, **랜덤 패턴**이 화면 전역에 분산
- 한 순간에 너무 빽빽하지 않도록 **밀도 제한**

### 5.3 속도/밀도
- **속도**: 매우 느린 낙하 (1~2px/frame 또는 8~20s당 화면 완주)
- **픽셀 스냅**: 낙하 위치/사이즈는 2~4px 단위로 맞춤
- **밀도**: 화면 폭 기준 4~8열 정도에서만 동시에 떨어짐
- **루프**: 바닥 도착 시 사라졌다가 새로운 패턴 생성

### 5.4 색상/레이어
- 색상: #8B0000 기반 + 30~60% 투명도
- 레이어: 타이틀 뒤쪽, 배경 위에 위치

---

## 6. Start 버튼 트랜지션

### 6.1 버튼 클릭 ("LET's FIX!")
- **글리치 파열**: 화면 중심에서 짧은 파열 라인/노이즈
- **화이트 플래시**: 0.2~0.35s 순간 점멸
- **파티클 폭발**: 사각형 파편 + 노이즈 스파크
- **사운드(후순위)**: 짧은 전자 폭발음

### 6.2 화면 전환
- **오버레이 페이드 아웃**: 0.4~0.6s
- **게임 시작**: 플래시 직후 즉시 전환

---

## 7. 연출 파라미터 기본값 (프리뷰 조정 가능)
- 타이틀 등장: 0.8s
- 글리치 전환: 0.6s
- _Error.exe 추가: 0.4s
- Idle 글리치 간격: 2~4s 랜덤
- 배경 낙하 속도: 8~20s/화면
- 플래시 길이: 0.2~0.35s

---

## 8. 체크리스트 (Implementation Sync)
- [ ] 타이틀 전용 폰트 추가 적용
- [ ] 글자 내부 CRT 노이즈/텍스처 적용
- [ ] 채널 분리 + 지터 + 스캔라인 붕괴 연출 구현
- [ ] 에러 블록 레인 랜덤 패턴 적용
- [ ] Start 버튼 클릭 시 글리치 파열 + 화이트 플래시
- [ ] 오버레이 페이드 아웃 이후 게임 시작

