# 📺 UI & Animation Design Document: Tetris_Error.exe (v2.0)

본 문서는 **Tetris_Error.exe**의 게임 내 화면 정보(UI) 배치 및 조작 상태, 스토리에 따른 인터랙션 규칙을 정의합니다.

---

## 1. 듀얼 조작 모드 UI 피드백 (Dual Mode UI)

게임의 가장 중요한 상태인 **캐릭터 모드**와 **블록(에러) 선택 모드**를 시각적으로 즉각 구분할 수 있도록 합니다.

### 1.1 플레이어 캐릭터 조종 모드 (기본)
* **커서**: 일반 조준선 또는 숨김.
* **화면 경계**: 일반 오락실 테트리스 배경.
* **HUD 강조**: 조작키 힌트 (`[W/SPACE] JUMP / [A][D] MOVE / [S] DROP`) 표출.

### 1.2 에러 블록 선택 모드 (해킹 상태)
* **진입 연출**: 마우스 좌클릭 타격 시 해당 블록에서 디지털 파편 파티클 발생. 카메라 미세 진동.
* **커서**: 블록 제어 중임을 나타내는 '잠금 십자선' 형태로 변경.
* **블록 변화**: 선택된 블록은 내부가 비어 있는 와이어프레임 또는 반투명(Opacity 0.4)으로 렌더링되며 1초 주기 점멸. 캐릭터가 통과 가능함을 직관적으로 보여줌.
* **화면 효과**: 화면 전체 테두리에 얇은 붉은색/보라색 글로우(Glow) 효과 유지.
* **HUD 조작키 변경**: `[W] ROTATE / [S] SOFT DROP / [SPACE] HARD DROP / [F] HOLD` 안내로 전환.

---

## 2. 스토리 컷신 및 대화창 (Dialog UI)

주인공이 오락실 게임기 내부에 빨려 들어간 상황과 코딩 에러(악마)와의 대립을 표현합니다.

### 2.1 대화창(Textbox) 구성
* **위치**: 화면 캔버스 최하단(또는 최상단)에 검은 박스 + 흰색 테두리 패널 팝업.
* **초상화**: 
  - 좌측: 주인공 (당황, 결의 등 표정 변화 도트 아이콘)
  - 우측: 악마/시스템 오류 (글리치 노이즈, 기괴한 형상 도트 아이콘)
* **텍스트 연출**: 타이핑 효과 (한 글자씩 출력). 중요 단어는 **붉은색** 등으로 진하게 강조.

### 2.2 컷신 발생 트리거
* **게임 시작 시 (Intro)**: 주인공이 빨려 들어오며 최초의 에러 블록 생성 전 대사 출력.
* **위기 상황 도달 시**: 맵의 최상단 가까이 블록이 도달하거나 타이머가 특정 수준을 넘겼을 때 악마의 도발 대사.
* **사망 (Game Over)**: 찌그러지는 연출과 함께 조롱하는 대사 후 메뉴로 복귀.

---

## 3. 정보 HUD (Head-Up Display)

기존 테트리스와 달리 점수/라인 클리어보다 **생존 시간**과 **보유 스킬(블록 조종)**이 중요하므로 패널을 정리합니다.

* **SURVIVAL TIME**: 화면 최상단 타이머 (`MM:SS.ms` 포맷). 가장 크게 렌더링.
* **HOLD BOX**: 마우스 선택 모드에서 `F`키로 타겟 블록을 잡았을 때 임시 보관되는 장소. (상시 노출)
* **NEXT BOX (선택적)**: 악마가 다음에 쏟아낼 블록들 미리보기. (선택 위협을 미리 파악)
* **WARNING / CRITICAL ZONE**: 위험 고도에 블록이 쌓이면 테두리 깜빡임 배속 증가.
