# Tetris_Error.exe 게임 기획 및 밸런스 조정 문서

이 문서는 `Tetris_Error.exe` 프로젝트의 전반적인 기획 명칭과 게임 내에서 직접 수정할 수 있는 수치(밸런스)들을 정리한 가이드입니다. 이 문서에 명시된 변수들을 `main.html` 파일 안에서 찾아 수정하면 게임의 난이도와 규칙을 자유롭게 바꿀 수 있습니다.

---

## 1. 핵심 기획 명칭 및 모드

- **프로젝트 명**: Tetris_Error.exe
- **장르**: 서바이벌 액션 플랫포머 (Survival Action Platformer) + 테트리스
- **플레이 루프**: 위에서 떨어지는 '에러(블록)'와 '레이저'를 피해 살아남으면서, 때로는 에러 블록을 '해킹'하여 직접 테트리스를 맞춰 파괴해야 합니다.

### 🎮 컨트롤 모드 (Control Modes)
1. **서바이벌 모드 (CHARACTER)**: 기본 상태입니다. 플레이어가 직접 캐릭터를 움직여(WASD) 떨어지는 블록과 레이저를 피하며 생존합니다.
2. **해킹 모드 (BLOCK)**: 붉은색 에러 블록을 **마우스로 클릭**하면 해당 블록에 대한 통제권을 뺏어 해킹 모드로 진입합니다. (이때 캐릭터는 제자리에 고정됩니다.) 해킹 모드에서는 블록을 이동(A/D), 회전(W), 하드드롭(Space), 소프트드롭(S), 홀드(F) 할 수 있습니다.

---

## 2. 점수 시스템 (Scoring System)

게임 내 점수 획득 공식은 `SCORE_CONFIG` 상수 객체를 통해 일괄 관리됩니다. 

```javascript
// main.html의 상수 선언부에서 찾을 수 있습니다.
const SCORE_CONFIG = {
    lineClear: [0, 100, 200, 300, 400], // 인덱스는 한 번에 지운 줄의 수입니다. (1줄 지우면 100점, 2줄 200점...)
    survivalPerSec: 10,                 // 캐릭터가 살아있는 동안 1초마다 지속적으로 오르는 점수
    softDropPerCell: 1,                 // 해킹 모드에서 블록을 부드럽게 내릴 때(S키) 1칸당 획득 점수
    hardDropPerCell: 2,                 // 해킹 모드에서 블록을 단번에 꽂을 때(Space키) 1칸당 획득 점수
    comboBonus: 50                      // 연속해서 라인을 클리어할 때 누적 스택당 추가되는 보너스 점수 (50 * 콤보)
};
```

---

## 3. 레벨 디자인 밸런스 (LEVELS)

스테이지별 목표 점수와 난이도 요소들은 `LEVELS` 배열을 통해 관리됩니다. 기획 의도에 맞춰 자유롭게 조율하세요.

```javascript
// 예시: 레벨 1 (BOOT SECTOR) 설정
{ 
    name: 'BOOT SECTOR', 
    targetScore: 10000,     // 다음 레벨로 넘어가기 위해 달성해야 하는 목표 점수 (만점)
    spawnInterval: 4.2,     // 에러 블록이 새롭게 떨어지는 주기 (초). 작을수록 자주 떨어집니다.
    dropInterval: 0.85,     // 일반 에러 블록이 1칸(30px) 낙하하는 데 걸리는 시간 (초). 작을수록 빠르게 떨어집니다.
    laserMin: 5.0,          // 레이저 공격이 발생하는 최소 쿨타임 (초)
    laserMax: 9.0,          // 레이저 공격이 발생하는 최대 쿨타임 (초)
    scoreMultiplier: 1.0    // 레벨 클리어 보정 배율. (위의 SCORE_CONFIG 점수들에 이 수치가 곱해집니다)
}
```

---

## 4. 플레이어(캐릭터) 물리 엔진 (Player Class)

캐릭터의 조작감(Game Feel)을 담당하는 물리 수치입니다. `class Player`의 `constructor` 안에 선언되어 있습니다.

- `this.speed = 220;` : 캐릭터의 좌우 이동 속도 (초당 픽셀 이동량)
- `this.jumpForce = -450;` : 점프 시 위로 솟아오르는 힘의 크기 (숫자가 클수록 높이 뜁니다. 음수 유지)
- `this.gravity = 1400;` : 기본 중력 값 (캐릭터가 아래로 떨어지는 가속도)
- `this.fastFallGravity = 3000;` : 점프 도중 `S`키를 눌렀을 때 발동하는 급강하 중력 값

---

## 5. 테트리스 룰 (Tetris Rules)

해킹 모드 시 테트리스 마니아들을 만족시킬 수 있는 전문가용 룰이 적용되어 있습니다. (코드 내부 하드코딩)
- **Lock Delay (2.0초)**: 블록이 바닥에 닿아도 바로 굳지 않고 최대 2.0초 동안 미끄러지거나 회전할 수 있습니다.
- **Infinity Rule (15회)**: 바닥에 닿은 상태에서 회전이나 이동을 성공시키면 락 딜레이 타이머가 초기화됩니다. 한 블록당 최대 15회까지만 연장 가능합니다.
- **DAS (150ms)**: 키보드 이동키를 꾹 누르고 있을 때 블록이 연속 이동을 시작하기 전까지의 딜레이
- **ARR (20ms)**: DAS 발동 후 블록이 초고속으로 이동하는 칸당 간격 (현재 초당 50칸 이동 속도)
- **SRS Wall Kick**: 벽 근처에서 블록을 회전할 때 겹치지 않게 위치를 밀어주는 시스템.

---

## 6. UI 및 화면 피드백 명칭

- **Hold Panel (좌측 UI)**: 해킹 모드 중 `F`키를 눌러 보관한 블록을 보여주는 공간.
- **Score Panel (우측 UI)**: 실시간 점수(SCORE), 레벨 클리어 목표(TARGET), 콤보(COMBO), 진행률 바(PROGRESS) 표기.
- **Spawn Warning Effect (경고 연출)**: 캔버스 상단 보이지 않는 구역(Hidden Rows)에 블록이 소환되었을 때, 완전히 화면에 보이기 전까지 위쪽 테두리에 붉은 네온과 그라데이션 빛이 퍼지며 사전 경고를 해주는 연출.
- **Danger Zone / Critical Zone**: 블록이 맵 상단 3칸 이내로 꽉 차오르면 배경이 붉게 맥동(Pulse)하며 압박감을 주는 이펙트.
- **Death Sequence (사망 연출)**: 플레이어가 블록에 깔리거나 레이저에 맞았을 때, 아주 느린 슬로우 모션과 함께 화면이 회전하며 강하게 줌-인 되는 극적인 연출. (`DEATH_DURATION = 1.6` 로 시간 조정 가능)
